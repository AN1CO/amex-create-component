import { Meta, Source, Canvas } from "@storybook/blocks";
import * as Modalstories from "./Modal.stories";

<Meta title="Modal" of={Modalstories} />

# Modal

A dialog box to put minimal content in.

<Canvas of={Modalstories.Basic} />

<Source code={`const TestModal = () => {
  const [openModal, setOpenModal] = useState(false);

return (

<>
  <button onClick={() => setOpenModal(true)}>Open Modal</button>
  <Modal isOpen={openModal} title="Test" onClose={() => setOpenModal(false)}>
    {children}
  </Modal>
</>
); };`} />
